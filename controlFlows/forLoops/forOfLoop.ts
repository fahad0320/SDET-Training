const arrForOf: string[] = ["Tutorialspoint", "JavaScript", "TypeScript"];
for (var element of arrForOf) {
   console.log(element);
}

// The for...of loop with strings

let strForOf: string = "Hello";
for (var char of strForOf) {
   console.log(char);
}