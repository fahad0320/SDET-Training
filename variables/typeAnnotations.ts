let username: string = "John"​  // string is being used for type annotation 

let age: number = 30​  // number is being used for type annotation 

let isLoggedIn: boolean = true​    // boolean is being used for type annotation